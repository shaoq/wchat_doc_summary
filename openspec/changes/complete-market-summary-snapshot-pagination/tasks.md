## 1. 修正全市场快照分页策略

- [x] 1.1 调整 `src/api/finance.py` 中 `_fetch_stock_snapshot()` 的分页逻辑，使用首页实际返回条数推导后续需要抓取的页数
- [x] 1.2 移除固定 `page <= 20` 的截断假设，改为动态上限与更稳健的终止条件
- [x] 1.3 为分页聚合加入按股票代码去重，确保完整性判定基于唯一股票数

## 2. 保持质量状态语义正确

- [x] 2.1 调整完整性判定逻辑，确保 `ok`、`partial`、`error` 分别对应完整抓取、尝试后仍不完整、首页失败/不可解析
- [x] 2.2 验证成交额和涨跌统计继续复用同一份完整快照，不引入新的重复抓取

## 3. 测试与回归保护

- [x] 3.1 更新 `tests/test_finance_contracts.py`，覆盖”上游忽略 `pz=5000`、每页仅返回 100 条、需要超过 20 页”的分页回归场景
- [x] 3.2 补充分页去重与”无新增记录即停止”的测试，防止重复页误判完整
- [x] 3.3 验证 `market-summary` 相关流程在完整分页成功时不再错误落入 `样本不完整 (2000/5518)` 路径
