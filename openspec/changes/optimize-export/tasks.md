## 1. 文件名与路径工具函数

- [ ] 1.1 实现 `sanitize_filename` 函数：替换文件系统不安全字符为 `_`，截断标题至 30 字符
- [ ] 1.2 实现 `build_export_filename` 函数：组合日期前缀 + 截断标题 + 重名序号处理，返回最终文件名
- [ ] 1.3 实现 `build_export_dir` 函数：返回 `output/export_articles/<mp_id>/` 的 Path 对象

## 2. Markdown 生成

- [ ] 2.1 实现 `build_article_markdown` 函数：按设计文档的结构生成单篇文章的 Markdown 内容（标题、元信息、摘要、正文），空字段省略

## 3. Export 命令重写

- [ ] 3.1 将 `export` 命令的 `--mp-id` option 改为 `mp_id` 必选 argument
- [ ] 3.2 移除 `--format` 和 `--output` 选项，新增 `--force` flag
- [ ] 3.3 实现核心导出逻辑：查询指定公众号的所有文章（按 publish_time DESC 排序）
- [ ] 3.4 实现增量逻辑：默认跳过已存在文件，记录跳过数量
- [ ] 3.5 实现 `--force` 逻辑：清空目标目录后全量导出
- [ ] 3.6 实现导出汇总：显示导出数量、跳过数量、输出目录路径

## 4. 验证

- [ ] 4.1 测试正常导出流程：指定有效 mp_id，验证文件结构和内容
- [ ] 4.2 测试增量导出：二次执行，验证已存在文件被跳过
- [ ] 4.3 测试 `--force`：验证目录被清空后重新导出
- [ ] 4.4 测试边界情况：无发布时间的文章、标题含特殊字符、空公众号
