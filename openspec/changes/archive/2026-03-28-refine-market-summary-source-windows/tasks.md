## 1. 修正默认交易日选择

- [x] 1.1 收紧 `market-summary` 默认 `trade_date` 逻辑，确保非交易日执行时回退到最近交易日
- [x] 1.2 校验交易日前开盘前场景，确保默认使用上一个交易日

## 2. 按资料类型拆分窗口语义

- [x] 2.1 为 CLS 看盘定义并接入 `trade_date 09:00 ~ trade_date 15:00` 窗口
- [x] 2.2 为 CLS 电报定义并接入 `trade_date 09:00 ~ next_trade_date 09:15` 窗口
- [x] 2.3 保持文章窗口为 `trade_date 15:00 ~ next_trade_date 09:15`，并统一走显式窗口计算

## 3. 对齐展示与查询行为

- [x] 3.1 让查询层和 CLI 展示层共用同一套 source-specific 窗口对象
- [x] 3.2 调整 CLI 输出，分别展示看盘、电报、文章的有效窗口

## 4. 补回归测试

- [x] 4.1 增加周末执行 `market-summary` 的默认 `trade_date` 测试
- [x] 4.2 增加看盘、电报、文章三类窗口的单元测试
- [x] 4.3 增加周五到周一场景的窗口边界测试
- [x] 4.4 运行相关测试并确认窗口语义与规格一致
