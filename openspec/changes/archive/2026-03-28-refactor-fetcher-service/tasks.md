## 1. 收敛 FetcherService 结构

- [x] 1.1 清理 `FetcherService` 中重复定义和职责错位的方法，固定公开接口为全量抓取、增量抓取、公众号信息解析和发布时间回填
- [x] 1.2 将全量抓取与增量抓取统一到共享的文章保存路径，避免重复保存逻辑
- [x] 1.3 调整相关 CLI 调用点，使 `subscribe` 和 `fetch` 依赖新的抓取服务接口语义

## 2. 修复抓取过滤与发布时间回填

- [x] 2.1 修复最近 N 天抓取的 cutoff 过滤与提前停止翻页逻辑
- [x] 2.2 明确并实现 `fetch_incremental()` 的最新文章时间比较与停止规则
- [x] 2.3 重写 `backfill_publish_time(mp_id)` 的参数处理、session 生命周期和最佳努力更新流程
- [x] 2.4 为发布时间缺失、无法解析时间和单篇文章抓取失败场景补充日志与容错

## 3. 验证与回归测试

- [x] 3.1 更新现有抓取服务测试，覆盖公众号信息解析、全量抓取和异常路径
- [x] 3.2 新增增量抓取与发布时间回填测试，验证 `mp_id` 语义、停止条件和部分失败容忍
- [x] 3.3 增加最小数据库级测试，验证抓取入库和回填逻辑在真实 session 下的行为
- [x] 3.4 运行相关回归测试并确认 `wchat fetch`、`wchat subscribe` 主路径行为符合新规范
