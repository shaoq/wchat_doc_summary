## 1. 修正默认交易日选择

- [x] 1.1 修正 `get_latest_trade_date()` 在 today 且非交易日场景下的回退逻辑
- [x] 1.2 验证交易日开盘前场景仍默认使用上一个交易日

## 2. 收紧失败即停止行为

- [x] 2.1 明确 `collect_market_data()` 返回的不可用状态判定标准
- [x] 2.2 调整 `market-summary` CLI，在市场数据不可用时停止后续阶段

## 3. 补回归测试

- [x] 3.1 增加周末默认 `trade_date` 的测试
- [x] 3.2 增加历史无数据和在线失败时 CLI 停止的测试
- [x] 3.3 运行相关测试并确认行为与规格一致
