## 1. 默认抓取语义调整

- [x] 1.1 调整 `src/cli/subscription.py` 的 `fetch` 默认路由，使未显式传 `--days` / `--full` 时改为“最新 10 条”模式，并同步更新 CLI 文案
- [x] 1.2 调整 `src/services/fetcher.py` 的抓取入口参数与默认流程，使默认模式只请求并处理单个公众号的最新 10 条文章，而显式 `--days` / `--full` 继续走现有范围语义

## 2. 上游异常上下文与窄窗口降级

- [x] 2.1 扩展 `src/api/weread.py` 的错误表达与日志输出，保留 HTTP 状态码和上游响应正文中的业务错误上下文
- [x] 2.2 在默认“最新 10 条”抓取模式中增加有限缩窗重试或等价的窄窗口降级策略，避免单条坏记录立即拖垮整次抓取

## 3. 测试与回归保护

- [x] 3.1 更新 `tests/test_services.py`，覆盖默认最新 10 条、显式 `--days` / `--full` 优先级以及默认模式的窄窗口降级行为
- [x] 3.2 更新 `tests/test_api.py` 或相关测试，覆盖上游列表失败时错误上下文透传而非仅保留泛化 `500` 消息
- [x] 3.3 视实现方式补充 `tests/test_fetcher_integration.py` 或 CLI 相关测试，验证默认输出文案与真实抓取模式一致
