## 1. 识别并拆分 CLI 命令边界

- [x] 1.1 梳理当前 `src/cli.py` 中的顶层命令、子命令和共享辅助函数
- [x] 1.2 设计领域命令模块边界，并确定哪些命令迁移到哪些模块
- [x] 1.3 提取统一入口与命令注册逻辑，避免所有命令继续堆在单文件中

## 2. 迁移命令定义并保持命令面稳定

- [x] 2.1 将顶层命令按领域迁移到新模块中并完成注册
- [x] 2.2 将 `ai` 及其子命令迁移到独立模块中并保持现有子命令名不变
- [x] 2.3 整理共享展示和辅助函数，避免模块间循环依赖

## 3. 补验证与回归测试

- [x] 3.1 增加顶层 `--help` 与 `ai --help` 的命令面回归测试
- [x] 3.2 验证现有命令入口在模块化后仍可正常调用
- [x] 3.3 运行 CLI 相关回归测试并确认用户可见命令行为未发生变化
