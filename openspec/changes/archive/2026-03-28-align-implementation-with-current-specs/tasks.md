## 1. 修复 CLI 入口兼容性

- [x] 1.2 为模块化 CLI 增加显式模块执行入口，恢复 `python -m src.cli` 可执行性
- [x] 1.2 校验模块入口与安装脚本入口共用同一 `main()` 命令树

## 2. 收敛交易日与文章窗口语义

- [x] 2.1 在 `MarketAnalyzer` 中抽出统一的 A 股交易日判断逻辑，排除周末调休交易日误判
- [x] 2.2 基于统一交易日逻辑实现下一交易日推导与精确文章时间窗口计算
- [x] 2.3 让文章查询、日志输出和 CLI 展示使用同一个时间窗口对象

## 3. 修正历史日期市场数据与缓存一致性

- [x] 3.1 识别并隔离仅支持当前行情的数据源调用，禁止其隐式服务历史交易日总结
- [x] 3.2 调整 `collect_market_data()` 与缓存服务，使历史交易日只使用对齐目标日期的数据或显式失败
- [x] 3.3 修正 `--force` 场景下的刷新语义，避免用当前行情覆盖历史交易日缓存

## 4. 补齐财联社新闻入库闭环

- [x] 4.1 为财联社电报增加稳定的抓取后入库路径，并保持去重语义
- [x] 4.2 为财联社看盘数据增加稳定的抓取后入库路径，并保持去重语义
- [x] 4.3 保持 `market-summary` 聚合层只读取本地持久化新闻数据，不直接拼接远端原始响应

## 5. 补规格导向回归测试

- [x] 5.1 增加交易日边界测试，覆盖周末、节假日和调休工作日周末场景
- [x] 5.2 增加历史交易日市场总结测试，覆盖缓存命中、无缓存失败和 `--force` 行为
- [x] 5.3 增加财联社新闻入库与聚合测试，确认本地入库后可被总结流程消费
- [x] 5.4 增加 `python -m src.cli` 与 `python -m src.cli ai --help` 回归测试
- [x] 5.5 运行相关测试并确认实现行为与当前 OpenSpec 规格一致
